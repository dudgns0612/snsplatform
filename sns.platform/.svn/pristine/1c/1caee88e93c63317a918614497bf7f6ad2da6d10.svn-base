package com.sns.biz.common;

import java.util.HashMap;

import javax.annotation.Resource;

import org.apache.commons.lang3.math.NumberUtils;
import org.apache.tomcat.jni.User;
import org.springframework.security.core.userdetails.UserDetails;
import org.springframework.security.core.userdetails.UserDetailsService;
import org.springframework.security.core.userdetails.UsernameNotFoundException;

import com.sns.biz.member.MemberDAO;

public class CustomUserDetailService implements UserDetailsService{

	@Resource private MemberDAO memberDao;
	
	@Override
	public UserDetails loadUserByUsername(String userNum) throws UsernameNotFoundException {
		
		HashMap<String,Object> userMap = memberDao.searchMember(NumberUtils.toInt(userNum));
		

		return null;
	}

}
